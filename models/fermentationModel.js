class Fermentation {
  constructor(id, temp, alc, densi, att, timestamp) {
    (this.id = id),
      (this.temp = temp),
      (this.alc = alc),
      (this.densi = densi),
      (this.att = att),
      (this.timestamp = timestamp);
  }
}

export default Fermentation;
